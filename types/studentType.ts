export type TStudent = {
  title: string;
  children?: any;
};

export enum ScheduleButton {
  PINK = "pink",
  BLACK = "black",
  PRIMARY = "primary",
  DEFAULT = "default",
  NODESIGN = "noDesign",
  LEMON = "lemon",
}
export enum ButtonSize {
  LARGE = "large",
  MEDIUM = "medium",
}
